// Comentário - Busca Binária
class BinarySearch {
    public static void main(String[] a) {
        System.out.println(new BS().Start(20));
    }
}

class BS {
    int[] number;
    int size;

    public int Start(int sz) {
        int aux;
        aux = this.Init(sz);
        return 0;
    }

    public int Init(int sz) {
        size = sz;
        number = new int[sz];
        return 0;
    }
}